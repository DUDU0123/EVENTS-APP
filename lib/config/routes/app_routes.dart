import 'package:flutter/material.dart';

class AppRoutes {
  static Map<String, Widget Function(BuildContext)> routes = {
    // AppRoutesName.home: (context)=> HomePage(),
    // AppRoutesName.login: (context)=> Login(),
    // AppRoutesName.description: (context)=> Description(),
  };
}